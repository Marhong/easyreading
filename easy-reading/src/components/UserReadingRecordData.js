import React,{Component} from 'react';

export default class UserReadingRecordData extends Component{
    constructor(props){
        super(props);
        this.state={

        }
        // 该组件创建时，发送请求至服务器，判断是否更新user_reading_data值
    }
    componentDidMount(){
        // 从服务器获取user_reading_data数据
    }
    render(){
        return(
            <div>

            </div>
        );
    }
}